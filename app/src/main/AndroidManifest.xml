<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="edu.pwap.pp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".activities.GetAllOrdersToDeliverActivity" />
        <activity android:name=".activities.GetAllDishCategoriesActivity" />
        <activity android:name=".activities.GetAllDishesActivity" />
        <activity android:name=".activities.AddDishActivity" />
        <activity android:name=".activities.CustomerViewActivity" />
        <activity android:name=".activities.WaiterViewActivity" />
        <activity android:name=".activities.DieticianViewActivity" />
        <activity android:name=".activities.AdminViewActivity" />
        <activity android:name=".activities.ConnectionErrorActivity" />
        <activity android:name=".activities.AddDishCategoryActivity" />
        <activity android:name=".activities.LoggingActivity" />
        <activity android:name=".activities.AddUserActivity" />
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>